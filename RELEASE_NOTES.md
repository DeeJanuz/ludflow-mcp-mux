# v0.4.0

- Migrate `analysis_stats` and `module_overview` renderers to built-in `rich_content` with renderer rules
- Remove `renderers/analysis-stats.js` and `renderers/module-overview.js` (~19KB reduction)
